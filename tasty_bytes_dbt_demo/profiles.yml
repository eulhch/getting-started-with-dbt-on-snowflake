tasty_bytes:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: demo_role
      database: tasty_bytes_dbt_db
      schema: dev
      warehouse: demo
    prod:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: demo_role
      database: tasty_bytes_dbt_db
      schema: prod
      warehouse: demo