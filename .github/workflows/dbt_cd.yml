name: dbt_CD

on:
  push:
    branches: [main]

jobs:
  cd_run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

<<<<<<< test_connection_branch
      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Snowflake CLI and dbt
        run: |
          pip install snowflake-cli-labs
          pip install dbt-snowflake
=======
      - name: Install Snowflake CLI
        uses: Snowflake-Labs/snowflake-cli-action@v1.1.0
>>>>>>> main

      - name: Deploy to Prod
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PRIVATE_KEY: ${{ secrets.SNOWFLAKE_PRIVATE_KEY }}
          SNOWFLAKE_DATABASE: tasty_bytes_dbt_db
          SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
        run: |
          snow dbt deps
          # This deploys to your 'prod' target defined in profiles.yml
          snow dbt deploy --target prod
